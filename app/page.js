export default function Home() {
  return (
    <div style={{ padding: '2rem', fontFamily: 'sans-serif' }}>
      <h1>Marketing API → BigQuery App</h1>
      <p>Use <code>/api/fetchData</code> to fetch and append data.</p>
    </div>
  );
}
